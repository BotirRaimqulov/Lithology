# Core data handling
numpy>=1.24
pandas>=2.0
pyyaml>=6.0
pyarrow>=14.0          # Parquet I/O for the dataset export

# Modeling (only required from Phase 9 onward / for training & inference)
torch>=2.1
scikit-learn>=1.3      # metrics: F1, PR-AUC, confusion matrices

# Visualization
matplotlib>=3.7

# Dev / testing
pytest>=7.4

# Tested against: numpy 2.4, pandas 3.0, pyarrow 25, torch 2.13,
# scikit-learn 1.9, matplotlib 3.11 (see tests/ -- 46 passing).
